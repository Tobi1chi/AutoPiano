Place `GeneralUser-GS.sf2` in this directory to serve it locally.

Expected default path:

- `/static/soundfonts/GeneralUser-GS.sf2`

If you prefer a different URL at runtime, set `window.__AUTOPIANO_GENERALUSER_SF2_URL__`
before booting the app.
